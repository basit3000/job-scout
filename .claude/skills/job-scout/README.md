# Job Scout skill

This skill runs against the **repository root** (parent of `.agents/`).

Use scripts as:
- `node scripts/fetch-jobs.mjs`
- `node scripts/build-evidence.mjs`
- `npm start` → http://localhost:4040

Canonical instructions: root `SKILL.md` (kept in sync with this copy).
