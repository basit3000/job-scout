{
  "_README": "Copy to search-profile.json (gitignored). Change \"market\" and optional cities for this person.",

  "market": "AE",
  "_market": "AE = UAE (default). Also: GB, US, DE, IN, SA. See markets/README.md to add more.",

  "boards": [
    {
      "board": "bayt",
      "_note": "MENA board (UAE/KSA). Skipped automatically for markets without baytCountry. Apify only.",
      "queriesFromProfile": true
    },
    {
      "board": "indeed",
      "_note": "Country taken from markets/<id>.json → indeedCountryCode / jobspyCountryIndeed.",
      "queriesFromProfile": true
    },
    {
      "board": "linkedin",
      "_note": "JobSpy first even when Allow paid (saves Apify). Apify only if that query returns 0. Default is pastWeek, then pastMonth if empty. Pin input.datePosted to disable widening.",
      "queriesFromProfile": true,
      "preferJobspy": true
    }
  ],

  "_moreBoards": "Optional/flaky: google, glassdoor (often blocked — leave off). DE free: arbeitsagentur, arbeitnow, berlinstartupjobs, munichstartup, pegel, nomado24, stepstone, xing, kimeta, heise, germantechjobs; zip_recruiter (US/CA), naukri (IN), bdjobs (BD). Toggle in the UI Portals tab.",

  "limitPerQuery": 10,
  "_limitPerQuery": "Jobs per title×city query. Raise it in the UI if you want a wider page. Each title×city×board can become one Apify run when paid.",

  "preferJobspy": true,
  "_preferJobspy": "Only applies when Allow paid is OFF. With --allow-paid / Allow paid, Apify runs first (JobSpy is fallback).",

  "maxApifyRuns": 8,
  "_maxApifyRuns": "Hard cap on paid Apify actor runs per fetch. 0 = unlimited.",

  "_cities": "Omit cities to use markets/<id>.json defaults. Override e.g. \"cities\": [{ \"where\": \"Berlin\", \"radiusKm\": 50 }]",

  "filters": {
    "maxAgeDays": 14,
    "countryOnly": true,
    "dropNationalsOnly": true,
    "_titlePatterns": "Pulled from profile.search.includeTitlePatterns / excludeTitlePatterns at runtime."
  },

  "cv": {
    "source": "local",
    "_source": "local = tailor from cv/resume.md. overleaf = pull/edit/push Overleaf .tex + PDF in prep pack.",
    "tailorMode": "agent",
    "_tailorMode": "agent = coding agent + cv-tailor. fast = keyword reorder only.",
    "agentProvider": "cursor",
    "_agentProvider": "cursor | claude-code | codex. Claude Code needs `claude` on PATH; Codex needs `codex` on PATH.",
    "agentModel": "composer-2.5",
    "_agentModel": "Model id for the provider (Cursor catalog, or CLI --model). Empty = CLI default for claude-code/codex.",
    "overleafPush": true,
    "_overleafPush": "When source is overleaf, commit+push tailored ats.tex/main.tex.",
    "updateMaster": false,
    "_updateMaster": "When source is local, also rewrite cv/resume.md with the tailored order (off = prep pack only)."
  }
}
